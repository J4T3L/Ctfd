name: "Reflected XSS Engine"
author: "Modul Ajar Jarkom"
category: "Web Exploitation"
description: |
  Mesin pencari dokumen ini merefleksikan kata kunci pencarian secara langsung ke dalam halaman tanpa sanitasi.
  Suntikkan skrip Reflected Cross-Site Scripting (XSS) seperti `<script>alert(1)</script>` untuk memicu eksekusi kode!

  **Target Link**: [https://target.paradick.my.id/app/xss](https://target.paradick.my.id/app/xss)

value: 100
type: standard
state: visible

flags:
  - "CTF{xss_r3fl3c73d_s3cr37_l34k_2026}"

tags:
  - web
  - xss
  - easy

hints:
  - content: "💡 Tool Recommended: `Burp Suite` / `nuclei` / `ffuf`. Fuzzing parameter `?q=` dengan payload XSS dasar `<script>alert(1)</script>` atau `"><img src=x onerror=alert(1)>`."
    cost: 10
